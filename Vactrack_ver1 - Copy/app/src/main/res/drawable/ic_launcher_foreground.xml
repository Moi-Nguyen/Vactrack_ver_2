<!-- app/src/main/res/drawable/ic_launcher_foreground.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 
        Adaptive icon foreground using logo.png
        The logo is centered and scaled to fit within the safe zone (inner 72dp of 108dp canvas)
    -->
    <item
        android:drawable="@drawable/logo"
        android:gravity="center"
        android:width="72dp"
        android:height="72dp" />
</layer-list>